node_modules
.turbo
.env
.env.local
dist
.next

# Local audit / notes (may contain sensitive detail); keep out of repo
TECHNICAL_AUDIT.md

# Local-only DB scripts (credentials / one-off); do not commit
packages/db/src/seed.ts
packages/db/src/seed-local-walls.ts
packages/db/src/migrate-to-r2.ts
packages/db/src/generate-embeddings.ts
packages/db/src/backfill-text-embeddings.ts
packages/db/src/seed-hindusm.ts

# Local wallpaper import source (large personal assets)
wallsCollection/

# Migration SQL files — project uses drizzle-kit push (schema-sync), not migrate runner.
# These files are never auto-applied; the schema is the source of truth.
packages/db/migrations/

# Xenova / @xenova/transformers model cache (large ONNX weights — never commit)
.cache/
packages/db/.cache/
services/api/.cache/